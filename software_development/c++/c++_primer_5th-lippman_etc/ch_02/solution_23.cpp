/*
 * Exercise 2.23, p. 56
 *
 * Given a pointer p, can you determine whether p points to a valid object?
 */

/*
 * Partially.
 * if p == nullptr then it definitely doesn't point to a valid object;
 * otherwise we can say nothing about if the pointed object is valids.
 */
