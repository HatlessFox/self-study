/*
 * Exercise 3.3, p. 90.
 *
 * Explain how whitespace characters are handled in the _string_ input
 * operator and in the _getline_ function.
 */

/*
 * input operator - reads a string till the first whitespace
 *                  (the ws is read, but not included in the string).
 * getline - read a string till the first "next line" symbol(s)
 *           (the symbol(s) is read, but not include in the result).
 */
